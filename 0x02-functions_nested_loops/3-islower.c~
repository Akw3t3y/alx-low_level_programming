#include <stdio.h>
/**
 *main - Entry point
 *
 *Desperation: Function that checks for lowercase character
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return 1;
} else {
return 0;
}
